# Core MCP dependencies
mcp[cli]>=1.0.0

# System and process management
psutil>=5.9.0

# HTTP requests and API calls
requests>=2.31.0
xmltodict>=0.13.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Azure and Power BI authentication
azure-identity>=1.14.0
azure-core>=1.29.0
msal>=1.24.0

# Database connectivity
pyodbc>=5.0.0

# Configuration management
python-dotenv>=1.0.0

# Windows-specific dependencies (TOM integration)
# These are optional and only needed for real-time Desktop integration
pythonnet>=3.0.1  # .NET CLR integration
# pywin32>=306      # Windows APIs (install separately on Windows)
# comtypes>=1.2.0   # COM objects (install separately on Windows)